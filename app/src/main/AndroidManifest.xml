<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 1. Standard Badí' Date & Schedule Widget -->
        <receiver
            android:name=".widget.BadiAppWidgetProvider"
            android:exported="true"
            android:label="@string/app_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/badi_widget_info" />
        </receiver>

        <!-- 2. Badí' Big Clock Widget -->
        <receiver
            android:name=".widget.BadiClockWidgetProvider"
            android:exported="true"
            android:label="@string/app_widget_big_clock_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/badi_clock_widget_info" />
        </receiver>

        <!-- 3. Badí' Agenda & Events Widget -->
        <receiver
            android:name=".widget.BadiAgendaWidgetProvider"
            android:exported="true"
            android:label="@string/app_widget_agenda_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/badi_agenda_widget_info" />
        </receiver>

        <!-- Service for Badí' Agenda Scrollable ListView -->
        <service
            android:name=".widget.BadiAgendaWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />

        <!-- 4. Badí' Compact Date Widget -->
        <receiver
            android:name=".widget.BadiCompactWidgetProvider"
            android:exported="true"
            android:label="@string/app_widget_compact_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/badi_compact_widget_info" />
        </receiver>
    </application>

</manifest>
